{{ form_start(form, {'method': 'GET'}) }}
{{ form_row(form) }}
<button type="submit">{{ 'sylius.ui.filter'|trans }}</button>
{{ form_end(form) }}

{% for product in resources %}
    {{ product.name }}
    <br>
{% endfor %}
